<?php foreach ( $this->rows as $row ) : ?>
    <tr>
	<td><?php echo $row->objective_desc; ?></td>
	<td><?php echo $row->objective_observation == 1 ? 'Loos' : 'Lae'; ?></td>
	<td><?php echo $row->objective_appointment_active == 1 ? 'Loos' : 'Lae'; ?></td>
	<td>
	    <?php if ( $this->access( Register_Form_AppointmentObjective::ID, Admin_Model_Mapper_SysUserHasForm::SAVE ) ) : ?>
		<a class="btn green" href="<?php echo $this->baseUrl( 'register/appointment-objective/edit/id/' . $row->id_appointment_objective ); ?>">
		    <i class="icon-edit"></i>  
		    Troka                                            
		</a>
	    <?php else : ?>
		<a class="btn red" href="javascript:;">
		    <i class="icon-alert icon-lock"></i>  
		    La bele acessu                                            
		</a>
	    <?php endif; ?>
	</td>
    </tr>
<?php endforeach; ?>