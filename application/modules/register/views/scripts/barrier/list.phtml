<?php foreach ( $this->rows as $row ) : ?>
    <tr>
	<td><?php echo $row->barrier_type_name; ?></td>
	<td><?php echo $row->barrier_name; ?></td>
	<td><?php echo $row->barrier_name_active == 1 ? 'Loos' : 'Lae'; ?></td>
	<td>
	    <?php if ( $this->access( Register_Form_Barrier::ID, Admin_Model_Mapper_SysUserHasForm::SAVE ) ) : ?>
		<a class="btn green" href="<?php echo $this->baseUrl( 'register/barrier/edit/id/' . $row->id_barrier_name ); ?>">
		    <i class="icon-edit"></i>  
		    Troka                                            
		</a>
	    <?php else : ?>
		<a class="btn red" href="javascript:;">
		    <i class="icon-alert icon-lock"></i>  
		    La bele acessu                                            
		</a>
	    <?php endif; ?>
	</td>
    </tr>
<?php endforeach; ?>