<?php foreach ( $this->rows as $row ) : ?>
    <tr>
	<td><?php echo $row->scholarity_area; ?></td>
	<td><?php echo $row->acronym; ?></td>
	<td>
	    <?php if ( $this->access( Register_Form_AreaScholarity::ID, Admin_Model_Mapper_SysUserHasForm::SAVE ) ) : ?>
		<a class="btn green" href="<?php echo $this->baseUrl( 'register/area-scholarity/edit/id/' . $row->id_scholarity_area ); ?>">
		    <i class="icon-edit"></i>  
		    Troka                                            
		</a>
	    <?php else : ?>
		<a class="btn red" href="javascript:;">
		    <i class="icon-alert icon-lock"></i>  
		    La bele acessu                                            
		</a>
	    <?php endif; ?>
	</td>
    </tr>
<?php endforeach; ?>