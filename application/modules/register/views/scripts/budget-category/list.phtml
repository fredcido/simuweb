<?php foreach ( $this->rows as $row ) : ?>
    <tr>
	<td><?php echo $row->description; ?></td>
	<td><?php echo $this->nomenclature()->typeBudgetCategory( $row->type ); ?></td>
	<td>
	    <?php if ( $this->access( Register_Form_BudgetCategory::ID, Admin_Model_Mapper_SysUserHasForm::SAVE ) ) : ?>
		<a class="btn green" href="<?php echo $this->baseUrl( 'register/budget-category/edit/id/' . $row->id_budget_category ); ?>">
		    <i class="icon-edit"></i>  
		    Troka                                            
		</a>
	    <?php else : ?>
		<a class="btn red" href="javascript:;">
		    <i class="icon-alert icon-lock"></i>  
		    La bele acessu                                            
		</a>
	    <?php endif; ?>
	</td>
    </tr>
<?php endforeach; ?>