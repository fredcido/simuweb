<?php foreach ( $this->rows as $row ) : ?>
    <tr>
	<td><?php echo $row->name; ?></td>
	<td><?php echo $row->login; ?></td>
	<td class="hidden-480"><?php echo $row->name_dec; ?></td>
	<td class="center hidden-480">
	    <span class="label label-<?php echo $row->active ? 'success' : 'warning'; ?>">
		<?php echo $row->active ? 'Sim' : 'Lae'; ?>
	    </span>
	</td>
	<td>
	    <?php if ( $this->access( Admin_Form_User::ID, Admin_Model_Mapper_SysUserHasForm::SAVE ) ) : ?>
		<a class="btn green" href="<?php echo $this->baseUrl( 'admin/user/edit/id/' . $row->id_sysuser ); ?>">
		    <i class="icon-edit"></i>  
		    Troka                                            
		</a>
	    <?php else : ?>
		<a class="btn red" href="javascript:;">
		    <i class="icon-alert icon-lock"></i>  
		    La bele acessu                                            
		</a>
	    <?php endif; ?>
	</td>
    </tr>
<?php endforeach; ?>