<?php foreach ( $this->rows as $row ) : ?>
    <tr>
	<td><?php echo Fefop_Model_Mapper_Contract::buildNumRow( $row ); ?></td>
	<td><?php echo $row->enterprise; ?></td>
	<td><?php echo $row->district; ?></td>
	<td><?php echo $row->area; ?></td>
	<td><?php echo $this->date( $row->date_start ); ?></td>
	<td><?php echo $this->date( $row->date_finish ); ?></td>
	<td><?php echo $this->currency( $row->amount ); ?></td>
	<td><?php echo $this->fefopContract()->renderStatusLabel( $row->id_fefop_status ); ?></td>
	<td>
	    <?php if ( empty( $this->listAjax ) ) : ?>
		<?php if ( $this->access( Fefop_Form_ETCContract::ID, Admin_Model_Mapper_SysUserHasForm::SAVE ) ) : ?>
		    <a class="btn green" href="<?php echo $this->baseUrl( 'fefop/etc-contract/edit/id/' . $row->id_per_contract ); ?>">
			<i class="icon-edit"></i>  
			Troka                                            
		    </a>
		<?php else : ?>
		    <a class="btn red" href="javascript:;">
			<i class="icon-alert icon-lock"></i>  
			La bele acessu                                            
		    </a>
		<?php endif; ?>
	    <?php else : ?>
		<a class="btn red action-ajax" href="javascript:;" data-id="<?php echo $row->id_per_contract; ?>">
		    <i class="icon-ok"></i>                                     
		</a>
	    <?php endif; ?>
	</td>
    </tr>
<?php endforeach; ?>