<?php foreach ( $this->rows as $row ) : ?>
    <tr>
	<td><?php echo $row->type_expense; ?></td>
	<td><?php echo $row->description; ?></td>
	<td style="width: 20%;">
	    <?php if ( $this->access( Fefop_Form_Expense::ID, Admin_Model_Mapper_SysUserHasForm::SAVE ) ) : ?>
		<a class="btn green" href="<?php echo $this->baseUrl( 'fefop/expense/edit/id/' . $row->id_budget_category ); ?>">
		    <i class="icon-edit"></i>  
		    Troka                                            
		</a>
		<?php if ( empty( $row->can_delete ) ) : ?>
		    <a class="btn red" href="javascript:;" onclick="Fefop.Expense.removeExpense(<?php echo $row->id_budget_category; ?>);">
			<i class="icon-remove-circle"></i>  
			Hamoos
		    </a>
		<?php endif; ?>
	    <?php else : ?>
		<a class="btn red" href="javascript:;">
		    <i class="icon-alert icon-lock"></i>  
		    La bele acessu                                            
		</a>
	    <?php endif; ?>
	</td>
    </tr>
<?php endforeach; ?>