<?php foreach ( $this->rows as $row ) : ?>
    <tr>
	<td><?php echo $row->type_handicapped; ?></td>
	<td><?php echo $row->handicapped; ?></td>
	<td><?php echo $row->needs; ?></td>
	<td>
	    <?php if ( $this->access( Client_Form_ClientHandicapped::ID, Admin_Model_Mapper_SysUserHasForm::SAVE ) ) : ?>
		<a class="btn red" data-handicapped='<?php echo $row->id_handicapped; ?>' onclick="Client.Client.removeHandicapped( this );">
		    <i class="icon-remove-sign"></i>            
		</a>
	    <?php else : ?>
		<a class="btn red" href="javascript:;">
		    <i class="icon-alert icon-lock"></i>  
		    La bele acessu                                            
		</a>
	    <?php endif; ?>
	</td>
    </tr>
<?php endforeach; ?>