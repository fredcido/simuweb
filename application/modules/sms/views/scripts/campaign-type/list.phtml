<?php foreach ( $this->rows as $row ) : ?>
    <tr>
	<td><?php echo $row->campaign_type; ?></td>
	<td>
	    <?php if ( $this->access( Sms_Form_CampaignType::ID, Admin_Model_Mapper_SysUserHasForm::SAVE ) ) : ?>
		<a class="btn green" href="<?php echo $this->baseUrl( 'sms/campaign-type/edit/id/' . $row->id_campaign_type ); ?>">
		    <i class="icon-edit"></i>  
		    Troka                                            
		</a>
	    <?php else : ?>
		<a class="btn red" href="javascript:;">
		    <i class="icon-alert icon-lock"></i>  
		    La bele acessu                                            
		</a>
	    <?php endif; ?>
	</td>
    </tr>
<?php endforeach; ?>